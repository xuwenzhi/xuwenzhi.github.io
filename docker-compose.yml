services:
  jekyll:
    build: .
    ports:
      - "4000:4000"    # site
      - "35729:35729"  # livereload
    volumes:
      - .:/site
      # Keep installed gems in the image; don't let the bind mount shadow them.
      - /site/.bundle
